'use strict';

describe('cvia.resultsView module', function() {

  beforeEach(module('cvia.resultsView'));

  describe('resultsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var resultsViewCtrl = $controller('resultsViewCtrl');
      expect(resultsViewCtrl).toBeDefined();
    }));

  });
});