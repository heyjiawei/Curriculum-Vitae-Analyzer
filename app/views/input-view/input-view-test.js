'use strict';

describe('cvia.inputView module', function() {

  beforeEach(module('cvia.inputView'));

  describe('inputView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var inputViewCtrl = $controller('inputViewCtrl');
      expect(inputViewCtrl).toBeDefined();
    }));

  });
});